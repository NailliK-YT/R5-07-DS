on: [push]
permissions:
  contents: write

jobs:
  create-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Créer Q5_archive_TOTO.txt
        run: |
            DATE=$(date +"%d-%m-%Y")
            FICHIER="Q5_archive_${DATE}.txt"
            echo "Killian Demeillers" > "$FICHIER"
            cat Q5.java >> "$FICHIER"
      - name: Commit et push
        run: |
          git config --global user.name "Killian Demeillers"
          git config --global user.email "killian.demeillers@etu.univ-lehavre.fr"
          git add "$FICHIER"
          git commit -m "Ajout rapport automatique"
          git push
